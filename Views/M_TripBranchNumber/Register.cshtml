@using ai_truck_load_measurement.Commons;
@model ai_truck_load_measurement.Models.M_TripBranchNumberModel;
@{
    Model.GetBaseView(User, ViewContext);
    ViewData["Title"] = "マスター - 便マスター - 便枝番マスター - 登録";
}

<div class="card shadow masterInfo m-4">

    <div class="card-header">
        @ViewData["Title"]
    </div>

    <div class="card-body">
        <div class="form-group col-sm-12 pl-3">
            <p id="div-error-message" class="mt-1 text-danger"></p>

            @if (ViewData["ErrorMessage"] != null)
            {
                <span class="mt-1 text-danger">@ViewData["ErrorMessage"]</span>
            }
        </div>
        <div class="row">
            <div class="col-lg-7 mb-2">
                <div class="card border-left-custom">
                    <div class="card-body">
                        <ul class="mb-0">
                            <li>適用開始日時は、適用開始日の前日の夜勤が終了する予定日時を入力してください。</li>
                            <li>同じ便名称の便マスターを登録する際は、適用開始/終了日時の範囲がかぶらないようにしてください。</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>

        <div class="mg-bot-20">
            <div class="col-16 sp-tablet mb-3">
                <form class="col-md-12" method="post" enctype="multipart/form-data" name="form" id="mTripBranchNumberForm">
                    <input hidden type="text" asp-for="TripID" id="tripID" required />
                    <div class="col-12 input-group div-input-group">
                        <div class="col-12 col-sm-4 col-md-2 column-1st">
                            <label>@Html.DisplayName("TripName")：</label>
                        </div>
                        <div class="col-12 col-sm-8 col-md-5 column-2nd">
                            <label>@Model.TripName</label>
                        </div>
                    </div>
                    <br />
                    <div class="col-12 input-group div-input-group">
                        <div class="col-12 col-sm-4 col-md-2 column-1st">
                            <label>@Html.DisplayName("RegistArrivalScheduledTime")<span style="color:red">*</span></label>
                        </div>
                        <div class="col-12 col-sm-8 col-md-5 column-2nd">
                            <input class="form-control" type="text" asp-for="RegistArrivalScheduledTime" autocomplete="off" id="ArrivalScheduledTime" required>
                            <small class="form-text text-muted w-100 p-1">hh:mmで入力してください</small>
                            <span class="text-danger w-100" asp-validation-for="RegistArrivalScheduledTime"></span>
                        </div>
                    </div>
                    <div class="col-12 input-group div-input-group">
                        <div class="col-12 col-sm-4 col-md-2 column-1st">
                            <label>@Html.DisplayName("RegistDepartureScheduledTime")<span style="color:red">*</span></label>
                        </div>
                        <div class="col-12 col-sm-8 col-md-5 column-2nd">
                            <input class="form-control" type="text" asp-for="RegistDepartureScheduledTime" autocomplete="off" id="DepartureScheduledTime" required>
                            <small class="form-text text-muted w-100 p-1">hh:mmで入力してください</small>
                            <span class="text-danger w-100" asp-validation-for="RegistDepartureScheduledTime"></span>
                        </div>
                    </div>
                    <div class="col-12 input-group div-input-group">
                        <div class="col-12 col-sm-4 col-md-2 column-1st">
                            <label>@Html.DisplayName("ApplicableStartDateTime")<span style="color:red">*</span></label>
                        </div>
                        <div class="col-12 col-sm-8 col-md-5 column-2nd">
                            <input class="form-control" type="datetime-local" asp-for="ApplicableStartDateTime" autocomplete="off" id="ApplicableStartDateTime" required>
                            <small class="form-text text-muted w-100 p-1">YYYY/MM/DD hh:mmで入力してください</small>
                            <span class="text-danger w-100" asp-validation-for="ApplicableStartDateTime"></span>
                        </div>
                    </div>
                    <div class="col-12 input-group div-input-group">
                        <div class="col-12 col-sm-4 col-md-2 column-1st">
                            <label>@Html.DisplayName("ApplicableEndDateTime")<span style="color:red">*</span></label>
                        </div>
                        <div class="col-12 col-sm-8 col-md-5 column-2nd">
                            <input class="form-control" type="datetime-local" asp-for="ApplicableEndDateTime" autocomplete="off" id="ApplicableEndDateTime" required>
                            <small class="form-text text-muted w-100 p-1">YYYY/MM/DD hh:mmで入力してください</small>
                            <span class="text-danger w-100" asp-validation-for="ApplicableEndDateTime"></span>
                        </div>
                    </div>
                    <div class="col-12 d-flex xs-block justify-content-start align-items-center p-0">
                        <div class="input-group-append mr-4">
                            <a class="btn btn-secondary btn-icon-split" asp-controller="M_TripBranchNumber" asp-action="Index"
                            asp-route-isChecked="@Model.IsCheckedBeforeApplicablePeriod" asp-route-tripId="@Model.TripID">
                                <span class="icon text-white-50">
                                    <i class="fas fa-arrow-left-long"></i>
                                </span>
                                <span class="text">戻る</span>
                            </a>
                        </div>
                        <div class="input-group-append">
                            <button class="btn btn-primary btn-icon-split" style="min-width: 80px" onclick="OnRegister()">
                                <span class="text">登録</span>
                            </button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
<script>

    // 登録ボタン押下
    function OnRegister() {

        var form = $("#mTripBranchNumberForm");

        form.submit(function (event) {

            $("#div-error-message").text("");

            // ページの更新を禁止する
            event.preventDefault();

            // 送信イベントが他のハンドラーに伝播するのを防ぐ
            event.stopImmediatePropagation();

            // フォーム情報取得
            let url = window.location.origin + '@Url.Action("Register", "M_TripBranchNumber")';
            let method = 'POST';
            let formId = 'mTripBranchNumberForm';
            let data = form.serializeArray();

            // Ajax call
            $.ajax({
                url: url,
                method: method,
                data: data
            }).done(function (response) {
                $('#' + formId)[0].reset();
                // 完了モーダル表示
                AlertMessage('', '完了', '登録が完了しました。', null, null);
            }).fail(function (jqXHR, textStatus, errorThrown) {
                console.log(jqXHR);
                if (jqXHR.status === 404) {
                    // データが見つからなかった場合
                    var errorMessage = jqXHR.responseJSON.errorMessage;
                    $("#div-error-message").text(errorMessage);
                } else {
                    // その他のエラーの場合
                    var errorMessage = 'E3002: サーバーに接続できませんでした。' + ' HttpRequest : ' + jqXHR.status + ' textStatus : ' + textStatus;
                    $("#div-error-message").text(errorMessage);
                }
            });
        });
    }
</script>
