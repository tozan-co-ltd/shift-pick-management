@using ai_truck_load_measurement.Commons;
@model ai_truck_load_measurement.Models.LoadOperationRecordModel;
@{
    Model.GetBaseView(User, ViewContext);
    ViewData["Title"] = Model.CategoryTitle + " - " + Model.ViewTitle;
}
<div class="card shadow de-inspection masterInfo m-4">
    <div class="card-header">
        @ViewData["Title"]
    </div>

    <div class="card-body">

        <div class="row">
            <div class="col-lg-10">
                <div class="card border-left-custom">
                    <div class="card-body">
                        <ul class="mb-0">
                            <li>最大20日分の稼働日を選択できます。</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>

        <div class="form-group col-sm-12 pl-3">
            <p id="div-error-message" class="mt-1 text-danger"></p>

            @if (ViewData["ErrorMessage"] != null)
            {
                <span class="mt-1 text-danger">@ViewData["ErrorMessage"]</span>
            }
        </div>

        <form id="loadOperationRecordForm">
            <div class="col-16 d-flex justify-content-start align-items-center unset-pdl sp-cs-dpl">
                <div class="d-flex justify-content-start align-items-center mb-3 unset-pdl">
                    <span class="warehouse-name-drs">稼働日</span>
                    <input type="text" id="workDay" class="form-control  pickerDate">
                </div>
            </div>
            <div class="col-16 d-flex justify-content-start align-items-center mb-3 unset-pdl mr-3 sp-cs-dpl" id="selectedWorkDays0">
                <span class="warehouse-name-drs">選択された稼働日</span>
            </div>
            <div class="col-16 d-flex justify-content-start align-items-center mb-3 unset-pdl mr-3 sp-cs-dpl" id="selectedWorkDays1">
                <span class="warehouse-name-drs"></span>
            </div>
            <div class="col-16 d-flex justify-content-start align-items-center mb-3 unset-pdl mr-3 sp-cs-dpl" id="selectedWorkDays2">
                <span class="warehouse-name-drs"></span>
            </div>
            <div class="col-16 d-flex justify-content-start align-items-center mb-3 unset-pdl mr-3 sp-cs-dpl" id="selectedWorkDays3">
                <span class="warehouse-name-drs"></span>
            </div>
            <div class="col-16 d-flex justify-content-start align-items-center unset-pdl sp-cs-dpl">
                <div class="d-flex justify-content-start align-items-center mb-3 unset-pdl mr-3">
                    <span class="warehouse-name-drs">便名称</span>
                    <select type="text" class="form-select " name="TripName" asp-for="TripName" asp-items="@Model.TripNameList" required id="tripName"></select>
                </div>
            </div>
            
        </form>


        <div class="col-16 d-flex justify-content-start align-items-center mb-3 unset-pdl">
            <button type="submit" class="btn btn-secondary btn-icon-split" id="search-btn" onclick="onSearchClick()">
                <span class="icon text-white-50">
                    <i class="fa-solid fa-magnifying-glass"></i>
                </span>
                <span class="text">検索</span>
            </button>
        </div>

        <div class="graph-and-table" id="graphAndTable">
            <hr />
            <div class="col-16 d-flex align-items-center mb-3 unset-pdl">
                <div class="mr-3"> 
                    <input type="checkbox" id="arrivalChartButton" name="chartCheckBoxes" value="arrival" checked />
                    <label for="arrivalChartButton">到着荷量</label>
                </div>

                <div>
                    <input type="checkbox" id="departureChartButton" name="chartCheckBoxes" value="departure" />
                    <label for="departureChartButton">出発荷量</label>
                </div>

                <button class="btn btn-secondary print-btn mr-4" id="search-btn" onclick="window.print()" style="margin-left: auto;">
                    <span class="text"><i class="fa-solid fa-print"></i></span>
                </button>
            </div>


            <div class="print-area" id="print-area">
                <div class="chartjs-graph chartjs-arrival" id="arrivalChart">
                    <canvas id="binedabanChartArrival"></canvas>
                    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.9.3/Chart.bundle.min.js" integrity="sha512-vBmx0N/uQOXznm/Nbkp7h0P1RfLSj0HQrFSzV8m7rOGyj30fYAOKHYvCNez+yM8IrfnW0TCodDEjRqf6fodf/Q==" crossorigin="anonymous"></script>
                    <script src='https://cdnjs.cloudflare.com/ajax/libs/chartjs-plugin-annotation/0.5.7/chartjs-plugin-annotation.min.js'></script>

                    <script>
                        var ctx = document.getElementById("binedabanChartArrival");
                        function adjustCanvasHeight() {
                            var canvas = document.getElementById("binedabanChartArrival");
                            var width = window.innerWidth;
                        }

                        window.addEventListener('resize', adjustCanvasHeight);

                        adjustCanvasHeight();

                        var arrivalChart = new Chart(ctx, {
                            type: 'scatter',
                            data: {
                                labels: [],
                                datasets: [{
                                    label: '豊鉄大型常傭便3_1',
                                    borderColor: 'Green',
                                    fill: false,
                                    data: [{ x: '2024-10-21T00:00:00', y: 5 }, { x: '2024-10-22T00:00:00', y: 55 }, { x: '2024-10-23T00:00:00', y: 45 }, { x: '2024-10-24T00:00:00', y: 25 }, { x: '2024-10-25T00:00:00', y: 65 }],
                                    tension: 0,
                                    pointRadius: '5',
                                    backgroundColor: 'Green',

                                },
                                {
                                    label: '豊鉄大型常傭便3_2',
                                    borderColor: 'Red',
                                    fill: false,
                                    data: [{ x: '2024-10-21T00:00:00', y: 95 }, { x: '2024-10-22T00:00:00', y: 75 }, { x: '2024-10-23T00:00:00', y: 25 }, { x: '2024-10-24T00:00:00', y: 55 }, { x: '2024-10-25T00:00:00', y: 15 }],
                                    tension: 0,
                                    pointRadius: '5',
                                    backgroundColor: 'Red',

                                }]
                            },
                            options: {
                                //凡例は表示
                                legend: {
                                    display: true
                                },
                                scales: {
                                    //X軸
                                    xAxes: [{
                                        id: 'x軸',
                                        //軸ラベル表示
                                        scaleLabel: {
                                            display: true,
                                            labelString: '稼働日',
                                            fontSize: 24
                                        },
                                        ticks: {
                                            fontSize: 20,
                                            min: '00:00',
                                            max: '23:59'
                                        },
                                        type: 'time',
                                        time: {
                                            parser: 'HH:mm',
                                            unit: 'hour',
                                            stepSize: 1,
                                            displayFormats: {
                                                'hour': 'HH:mm'
                                            }
                                        },
                                    }],
                                    //Y軸
                                    yAxes: [{
                                        //軸ラベル表示
                                        scaleLabel: {
                                            display: true,
                                            labelString: '到着荷量(%)',
                                            fontSize: 24
                                        },
                                        //Y軸の範囲を指定
                                        ticks: {
                                            min: 0.0,
                                            max: 100.0,
                                            fontSize: 20
                                        }
                                    }]
                                }, 
                                annotation: {
                                    annotations: [{           
                                        type: 'line', // 線分を指定
                                        drawTime: 'afterDraw',
                                        id: 'a-line-1', // 線のid名を指定（他の線と区別するため）
                                        mode: 'vertical', // 垂直を指定
                                        scaleID: 'x軸', // 基準とする軸のid名
                                        value: '12:00', // 引きたい線の数値（始点）
                                        borderColor: 'black', // 線の色
                                        borderWidth: 3, // 線の幅（太さ）
                                        borderDash: [2, 2],
                                        borderDashOffset: 1,
                                    }]
                                },
                                title: {
                                    display: true,
                                    text: '実績-荷量推移',
                                    fontSize: 30
                                }
                            }
                        });
                    </script>
                </div>
                <div class="chartjs-graph chartjs-departure" id="departureChart">
                    <canvas id="binedabanChartDeparture"></canvas>
                    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.9.3/Chart.bundle.min.js" integrity="sha512-vBmx0N/uQOXznm/Nbkp7h0P1RfLSj0HQrFSzV8m7rOGyj30fYAOKHYvCNez+yM8IrfnW0TCodDEjRqf6fodf/Q==" crossorigin="anonymous"></script>
                    <script src='https://cdnjs.cloudflare.com/ajax/libs/chartjs-plugin-annotation/0.5.7/chartjs-plugin-annotation.min.js'></script>
                    <script>
                        var ctx = document.getElementById("binedabanChartDeparture");
                        function adjustCanvasHeight() {
                            var canvas = document.getElementById("binedabanChartDeparture");
                            var width = window.innerWidth;
                        }

                        window.addEventListener('resize', adjustCanvasHeight);

                        adjustCanvasHeight();

                        var departureChart = new Chart(ctx, {
                            type: 'scatter',
                            data: {
                                labels: [],
                                datasets: [{
                                    label: '豊鉄大型常傭便3_1',
                                    borderColor: 'Green',
                                    fill: false,
                                    data: [{ x: '2024-10-21T00:00:00', y: 5 }, { x: '2024-10-22T00:00:00', y: 55 }, { x: '2024-10-23T00:00:00', y: 45 }, { x: '2024-10-24T00:00:00', y: 25 }, { x: '2024-10-25T00:00:00', y: 65 }],
                                    tension: 0,
                                    pointRadius: '5',
                                    backgroundColor: 'Green',

                                },
                                {
                                    label: '豊鉄大型常傭便3_2',
                                    borderColor: 'Red',
                                    fill: false,
                                    data: [{ x: '2024-10-21T00:00:00', y: 95 }, { x: '2024-10-22T00:00:00', y: 75 }, { x: '2024-10-23T00:00:00', y: 25 }, { x: '2024-10-24T00:00:00', y: 55 }, { x: '2024-10-25T00:00:00', y: 15 }],
                                    tension: 0,
                                    pointRadius: '5',
                                    backgroundColor: 'Red',

                                }]
                            },
                            options: {
                                //凡例は表示
                                legend: {
                                    display: true
                                },
                                scales: {
                                    //X軸
                                    xAxes: [{
                                        id: 'x軸',
                                        //軸ラベル表示
                                        scaleLabel: {
                                            display: true,
                                            labelString: '稼働日',
                                            fontSize: 24
                                        },
                                        ticks: {
                                            fontSize: 20,
                                            min: '00:00',
                                            max: '23:59'
                                        },
                                        type: 'time',
                                        time: {
                                            parser: 'HH:mm',
                                            unit: 'hour',
                                            stepSize: 1,
                                            displayFormats: {
                                                'hour': 'HH:mm'
                                            }
                                        },
                                    }],
                                    //Y軸
                                    yAxes: [{
                                        //軸ラベル表示
                                        scaleLabel: {
                                            display: true,
                                            labelString: '出発荷量(%)',
                                            fontSize: 24
                                        },
                                        //Y軸の範囲を指定
                                        ticks: {
                                            min: 0.0,
                                            max: 100.0,
                                            fontSize: 20
                                        }
                                    }]
                                },
                                annotation: {
                                    annotations:[]
                                },
                                title: {
                                    display: true,
                                    text: '実績-荷量推移',
                                    fontSize: 30
                                }
                            }
                        });
                    </script>
                </div>
                <div class="chartjs-graph chartjs-all" id="allChart">
                    <canvas id="binedabanChartAll"></canvas>
                    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.9.3/Chart.bundle.min.js" integrity="sha512-vBmx0N/uQOXznm/Nbkp7h0P1RfLSj0HQrFSzV8m7rOGyj30fYAOKHYvCNez+yM8IrfnW0TCodDEjRqf6fodf/Q==" crossorigin="anonymous"></script>
                    <script src='https://cdnjs.cloudflare.com/ajax/libs/chartjs-plugin-annotation/0.5.7/chartjs-plugin-annotation.min.js'></script>

                    <script>
                        var ctx = document.getElementById("binedabanChartAll");
                        function adjustCanvasHeight() {
                            var canvas = document.getElementById("binedabanChartAll");
                            var width = window.innerWidth;
                        }

                        window.addEventListener('resize', adjustCanvasHeight);

                        adjustCanvasHeight();

                        var allChart = new Chart(ctx, {
                            type: 'scatter',
                            data: {
                                labels: [],
                                datasets: [{
                                    label: '豊鉄大型常傭便3_1',
                                    borderColor: 'Green',
                                    fill: false,
                                    data: [{ x: '2024-10-21T00:00:00', y: 5 }, { x: '2024-10-22T00:00:00', y: 55 }, { x: '2024-10-23T00:00:00', y: 45 }, { x: '2024-10-24T00:00:00', y: 25 }, { x: '2024-10-25T00:00:00', y: 65 }],
                                    tension: 0,
                                    pointRadius: '5',
                                    backgroundColor: 'Green',

                                },
                                {
                                    label: '豊鉄大型常傭便3_2',
                                    borderColor: 'Red',
                                    fill: false,
                                    data: [{ x: '2024-10-21T00:00:00', y: 95 }, { x: '2024-10-22T00:00:00', y: 75 }, { x: '2024-10-23T00:00:00', y: 25 }, { x: '2024-10-24T00:00:00', y: 55 }, { x: '2024-10-25T00:00:00', y: 15 }],
                                    tension: 0,
                                    pointRadius: '5',
                                    backgroundColor: 'Red',

                                }]
                            },
                            options: {
                                //凡例は表示
                                legend: {
                                    display: true
                                },
                                scales: {
                                    //X軸
                                    xAxes: [{
                                        id: 'x軸',
                                        //軸ラベル表示
                                        scaleLabel: {
                                            display: true,
                                            labelString: '稼働日',
                                            fontSize: 24
                                        },
                                        ticks: {
                                            fontSize: 20,
                                            min: '00:00',
                                            max: '23:59'
                                        },
                                        type: 'time',
                                        time: {
                                            parser: 'HH:mm',
                                            unit: 'hour',
                                            stepSize: 1,
                                            displayFormats: {
                                                'hour': 'HH:mm'
                                            }
                                        },
                                    }],
                                    //Y軸
                                    yAxes: [{
                                        //軸ラベル表示
                                        scaleLabel: {
                                            display: true,
                                            labelString: '荷量(%)',
                                            fontSize: 24
                                        },
                                        //Y軸の範囲を指定
                                        ticks: {
                                            min: 0.0,
                                            max: 100.0,
                                            fontSize: 20
                                        }
                                    }]
                                },
                                annotation: {
                                    annotations: [{
                                        type: 'line', // 線分を指定
                                        drawTime: 'afterDraw',
                                        id: 'a-line-1', // 線のid名を指定（他の線と区別するため）
                                        mode: 'vertical', // 垂直を指定
                                        scaleID: 'x軸', // 基準とする軸のid名
                                        value: '12:00', // 引きたい線の数値（始点）
                                        borderColor: 'black', // 線の色
                                        borderWidth: 3, // 線の幅（太さ）
                                        borderDash: [2, 2],
                                        borderDashOffset: 1,
                                    }]
                                },
                                title: {
                                    display: true,
                                    text: '実績-荷量推移',
                                    fontSize: 30
                                }
                            }
                        });
                    </script>
                </div>
                <div class="chartjs-graph chartjs-clear" id="clearChart">
                    <canvas id="binedabanChartClear"></canvas>
                    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.9.3/Chart.bundle.min.js" integrity="sha512-vBmx0N/uQOXznm/Nbkp7h0P1RfLSj0HQrFSzV8m7rOGyj30fYAOKHYvCNez+yM8IrfnW0TCodDEjRqf6fodf/Q==" crossorigin="anonymous"></script>
                    <script src='https://cdnjs.cloudflare.com/ajax/libs/chartjs-plugin-annotation/0.5.7/chartjs-plugin-annotation.min.js'></script>
                    <script>
                        var ctx = document.getElementById("binedabanChartClear");
                        function adjustCanvasHeight() {
                            var canvas = document.getElementById("binedabanChartClear");
                            var width = window.innerWidth;
                        }

                        window.addEventListener('resize', adjustCanvasHeight);

                        adjustCanvasHeight();

                        var clearChart = new Chart(ctx, {
                            type: 'scatter',
                            data: {
                                labels: [],
                                datasets: []
                            },
                            options: {
                                //凡例は表示
                                legend: {
                                    display: true
                                },
                                scales: {
                                    //X軸
                                    xAxes: [{
                                        id: 'x軸',
                                        //軸ラベル表示
                                        scaleLabel: {
                                            display: true,
                                            labelString: '稼働日',
                                            fontSize: 24
                                        },
                                        ticks: {
                                            fontSize: 20,
                                            min: '00:00',
                                            max: '23:59'
                                        },
                                        type: 'time',
                                        time: {
                                            parser: 'HH:mm',
                                            unit: 'hour',
                                            stepSize: 1,
                                            displayFormats: {
                                                'hour': 'HH:mm'
                                            }
                                        },
                                    }],
                                    //Y軸
                                    yAxes: [{
                                        //軸ラベル表示
                                        scaleLabel: {
                                            display: true,
                                            labelString: '荷量(%)',
                                            fontSize: 24
                                        },
                                        //Y軸の範囲を指定
                                        ticks: {
                                            min: 0.0,
                                            max: 100.0,
                                            fontSize: 20
                                        }
                                    }]
                                },
                                annotation: {
                                    annotations: []
                                },
                                title: {
                                    display: true,
                                    text: '実績-荷量推移',
                                    fontSize: 30
                                }
                            }
                        });
                    </script>
                </div>
            </div>
            
            <hr />
            <div class="mg-bot-20 mt-3">
                <div class="col-12 d-flex xs-block justify-content-start align-items-center p-0">
                    <div class="input-group-append mr-1">
                        <a href="#" class="btn btn-info btn-icon-split" onclick="onExportFileTrips('LoadOperationRecord', '@Model.ViewTitle')" id="exportFile">
                            <span class="icon text-white-50">
                                <i class="fas fa-file-export"></i>
                            </span>
                            <span class="text">Excel出力</span>
                        </a>
                    </div>
                    <div class="input-group-append mr-3">
                        <a href="#" class="btn btn-info btn-icon-split" onclick="onExportAllImages()">
                            <span class="icon text-white-50">
                                <i class="fa-solid fa-circle-down"></i>
                            </span>
                            <span class="text">画像一括出力</span>
                        </a>
                    </div>
                </div>
            </div>
            <div class="mt-3" id="tripRecordTable"></div>
        </div>
    </div>
</div>

<!--  Detail Modal-->
<div class="modal fade" id="detail-modal" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content trip-record-modal">
            <div class="modal-header">
                <h5 class="modal-title" id="detail-modal-label"></h5>
                <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="col-12 col-md-12">
                    <p id="edit-modal-error-message" class="mt-1 text-danger"></p>
                </div>
                <figure class="mb-2 modal-img"><img src="" id="modalImage" /></figure>
                <div id="tripRecord-buttons"></div>
                <table class="table table-sm p-2 modal-table">
                    <tbody id="tripRecord-contatiner">
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<script>
    let model;

    $(document).ready(function () {
        model = @Html.Raw(Json.Serialize(Model));
        console.log(model);
        var today = new Date();
        $('#workDay').val(GetDayString(today));
        var y = today.getFullYear();
    })

    // 稼働日の日付変更時
    $('#workDay').change(function () {
        // 稼働日の取得
        var workDay = new Date($('#workDay').val());
        var workDayString = GetDayString(workDay);
        addWorkDays(workDayString);
        periodChange();
    });

    // 選択された稼働日の配列
    let arrayWorkDays = [];

    // 稼働日変更時
    function periodChange() {
        

        // 便名称の選択肢を全削除
        var select = $('#tripName')[0];
        while (select.firstChild) {
            select.removeChild(select.firstChild);
        }

        // フォーム情報取得
        let url = window.location.origin + '@Url.Action("GetTripNameFromWorkDay", "LoadOperationRecord")';
        let method = 'Post';
        let data = { workDays: arrayWorkDays};

        // Ajax call
        $.ajax({
            url: url,
            method: method,
            data: data
        }).done(function (response) {
            for (var i = 0; i < response.length; i++) {
                var option = document.createElement("option");
                if (response[i].text == null) {
                    option.text = "";
                }
                else {
                    option.text = response[i].text;
                }
                option.value = response[i].value;
                select.appendChild(option);
            }

        }).fail(function (jqXHR, textStatus, errorThrown) {
            if (jqXHR.status === 404) {
                var errorMessage = jqXHR.responseJSON.errorMessage;
                $("#div-error-message").text(errorMessage);
            } else {
                var errorMessage = 'E3002: サーバーに接続できませんでした。' + ' HttpRequest : ' + jqXHR.status + ' textStatus : ' + textStatus;
                $("#div-error-message").text(errorMessage);
            }
        });
    }

    

    // 追加ボタン押下時
    function addWorkDays(workDay) {
        // 20件の便が選択されているとき
        if (20 <= arrayWorkDays.length) {
            var errorMessage = 'E1008: 選択できる稼働日は最大20件です。20件を超えないように選択してください。';
            $("#div-error-message").text(errorMessage);
            return;
        }
       
        // 重複チェック
        if (arrayWorkDays.find(x => x == workDay)) {
            return;
        }

        // 選択された稼働日を追加する行を設定
        var selectedWorkDaysNumber = Math.floor(arrayWorkDays.length / 5);
        var selectedWorkDays = $('#selectedWorkDays' + selectedWorkDaysNumber)[0];

        // 選択された稼働日に追加
        arrayWorkDays.push(workDay);
        pushLabelToSelectedWorkDays(selectedWorkDays, workDay);
    }

    // 選択された便ラベルを作成、指定した親要素の子として登録
    function pushLabelToSelectedWorkDays(selectedWorkDays, workDay) {
        var selectedWorkDayLabel = document.createElement("label");
        selectedWorkDayLabel.innerText = workDay;
        selectedWorkDayLabel.innerHTML += "<a href=\"#\" class=\"label-delete ml-1\" onclick=\"onLabelDeleteClick('" + workDay + "')\" id=\"\">×</a>";
        selectedWorkDayLabel.className += "mr-2 selected-trip-label";
        selectedWorkDayLabel.style.backgroundColor = "rgba(200, 200, 200, 0.6)";
        selectedWorkDayLabel.style.padding = "0.5em";
        selectedWorkDays.appendChild(selectedWorkDayLabel);
    }

    // 選択された便の×ボタン押下時
    function onLabelDeleteClick(workDayString) {
        // ラベル全削除
        $('.selected-trip-label').remove();

        // 配列から削除
        var deleteIndex = arrayWorkDays.indexOf(workDayString);
        arrayWorkDays.splice(deleteIndex, 1);

        // 選択された便の再表示
        for (var i = 0; i < arrayWorkDays.length; i++) {
            // 選択された便を追加する行を設定
            var selectedWorkDaysNumber = Math.floor(i / 5);
            var selectedWorkDays = $('#selectedWorkDays' + selectedWorkDaysNumber)[0];

            var selectedWorkDayName = arrayWorkDays[i];
            pushLabelToSelectedWorkDays(selectedWorkDays, selectedWorkDayName);
        }

        // セレクトリスト更新
        periodChange();
    }

    let labelAppearedTime = 0;

    // 検索ボタン押下時
    function onSearchClick() {
        DeleteErrorMessages();

        // 便名称取得
        var tripName = $('[name=TripName]').val();

        if(tripName == null){
            return;
        }

        // グラフとテーブルの表示
        $('#graphAndTable').css('display', 'block');
        tableDisplay();
        console.log($('#tripRecordDataTable_filter'));

        // 初期表示は到着荷量
        $('input[value="arrival"]').prop('checked', true);
        $('input[value="departure"]').prop('checked', false);
        $('#arrivalChart').css('display', 'block');
        $('#departureChart').css('display', 'none');
        $('#allChart').css('display', 'none');
        $('#clearChart').css('display', 'none');

        // 昼勤開始時間保存用
        var dayShiftStartTime = "";

        // グラフの初期化
        arrivalChart.data.datasets = [];
        departureChart.data.datasets = [];
        allChart.data.datasets = [];

        // 追加された便、便枝番について
        for (var i = 0; i < arrayWorkDays.length; i++) {
            var workDay = arrayWorkDays[i];

            // フォーム情報取得
            let url = window.location.origin + '@Url.Action("SearchTrips", "LoadOperationRecord")';
            let method = 'Post';
            let data = { workDay: workDay, tripName: tripName };

            // Ajax call
            $.ajax({
                url: url,
                method: method,
                data: data,
                async: false
            }).done(function (response) {
                // 荷量クラスをパーセントとして取得
                var arrivalLoadStatuses = [];
                var departureLoadStatuses = [];
                var allLoadStatuses = [];

                response.forEach(function (loadRecord) {
                    var arrivalLoadStatus = loadRecord.arrivalLoadStatus;
                    var departureLoadStatus = loadRecord.departureLoadStatus;
                    if (arrivalLoadStatus != "") {
                        var arrivedAt = GetTimeString(new Date(loadRecord.arrivedAt));
                        arrivalLoadStatuses.push({ x: arrivedAt, y: arrivalLoadStatus });
                        allLoadStatuses.push({ x: arrivedAt, y: arrivalLoadStatus });
                    }

                    if (departureLoadStatus != "") {
                        var departedAt = GetTimeString(new Date(loadRecord.departedAt));
                        departureLoadStatuses.push({ x: departedAt, y: departureLoadStatus });
                        allLoadStatuses.push({ x: departedAt, y: departureLoadStatus })
                    }

                    dayShiftStartTime = GetTimeString(new Date(loadRecord.dayShiftStartTime));
                })


                // // データセット作成、登録
                var arrivalDatasets = createChartDatasets(i);
                arrivalDatasets.data = arrivalLoadStatuses;
                arrivalDatasets.label = GetDayString(new Date(workDay));
                arrivalChart.data.datasets.push(arrivalDatasets);

                var departureDatasets = createChartDatasets(i);
                departureDatasets.data = departureLoadStatuses;
                departureDatasets.label = GetDayString(new Date(workDay));
                departureChart.data.datasets.push(departureDatasets);

                var allDatasets = createChartDatasets(i);
                allDatasets.data = allLoadStatuses;
                allDatasets.label = GetDayString(new Date(workDay));
                allChart.data.datasets.push(allDatasets);
                
                var clearDatasets = createChartDatasets(i);
                clearDatasets.label = GetDayString(new Date(workDay));
                clearChart.data.datasets.push(clearDatasets);
               

            }).fail(function (jqXHR, textStatus, errorThrown) {
                if (jqXHR.status === 404) {
                    var errorMessage = jqXHR.responseJSON.errorMessage;
                    $("#div-error-message").text(errorMessage);
                } else {
                    var errorMessage = 'E3002: サーバーに接続できませんでした。' + ' HttpRequest : ' + jqXHR.status + ' textStatus : ' + textStatus;
                    $("#div-error-message").text(errorMessage);
                }
            });
        } 

        // 昼勤開始時間の線の初期化
        arrivalChart.options.annotation.annotations = []; 
        departureChart.options.annotation.annotations = [];
        allChart.options.annotation.annotations = [];

        // 昼勤開始時間の線描画用データ作成
        var annotationLine = createChartAnnotation(dayShiftStartTime, labelAppearedTime);
        labelAppearedTime++;

        // 昼勤開始時間の線追加
        arrivalChart.options.annotation.annotations.push(annotationLine);
        departureChart.options.annotation.annotations.push(annotationLine);
        allChart.options.annotation.annotations.push(annotationLine);
        clearChart.options.annotation.annotations.push(annotationLine);

        // グラフのタイトル設定
        arrivalChart.options.title.text = tripName;
        departureChart.options.title.text = tripName; 
        allChart.options.title.text = tripName;
        clearChart.options.title.text = tripName;


        // グラフの更新
        arrivalChart.update();
        departureChart.update();
        allChart.update();
        clearChart.update();
    }

    // chartjs用のデータセット作成
    function createChartDatasets(dataNumber) {
        var datasets = {
            label: '',
            borderColor: graphColors[dataNumber],
            fill: false,
            data: [],
            tension: 0,
            pointRadius: '5',
            backgroundColor: graphColors[dataNumber],
            spanGaps: true,
        }
        return datasets;
    }

    // chartjs用の線描画用データ作成
    function createChartAnnotation(dayShiftStartTime, labelAppearedTime){
        var annotationLine = {
            type: 'line', // 線分を指定
            drawTime: 'afterDatasetsDraw',
            id: labelAppearedTime.toString(), // 線のid名を指定（他の線と区別するため）
            mode: 'vertical', // 垂直を指定
            scaleID: 'x軸', // 基準とする軸のid名
            value: dayShiftStartTime, // 引きたい線の数値（始点）
            borderColor: 'black', // 線の色
            borderWidth: 3, // 線の幅（太さ）
            // borderDash: [2, 2],
            // borderDashOffset: 1,
        };
        return annotationLine;
    }

    // チェックボックス切り替え時
    $(function () {
        $('input[name="chartCheckBoxes"]:checkBox').change(function () {
            // デフォルトの操作を無効化
            event.preventDefault();

            var checkBoxVal = "";

            $('input:checked').each(function () {
                var r = $(this).val();
                checkBoxVal += r;
                console.log(r);
            })
            if (checkBoxVal == "arrival") {
                $('#arrivalChart').css('display', 'block');
                $('#departureChart').css('display', 'none');
                $('#allChart').css('display', 'none');
                $('#clearChart').css('display', 'none');
            }
            else if (checkBoxVal == "departure") {
                $('#departureChart').css('display', 'block');
                $('#arrivalChart').css('display', 'none');
                $('#allChart').css('display', 'none');
                $('#clearChart').css('display', 'none');
            }
            else if(checkBoxVal == "arrivaldeparture"){
                $('#arrivalChart').css('display', 'none');
                $('#departureChart').css('display', 'none');
                $('#allChart').css('display', 'block');
                $('#clearChart').css('display', 'none');
            }
            else{
                $('#arrivalChart').css('display', 'none');
                $('#departureChart').css('display', 'none');
                $('#allChart').css('display', 'none');
                $('#clearChart').css('display', 'block');
            }
        })
    })

    // 日付の表示形式変換(yyyy/MM/dd)
    function GetDayString(date) {
        var day = date.toLocaleDateString("ja-JP", {
            year: "numeric", month: "2-digit",
            day: "2-digit"
        });
        return day;
    }

    // 便実績テーブル表示
    function tableDisplay() {
        // 便名称取得
        var tripName = $('[name=TripName]').val();

        // フォーム情報取得
        let url = window.location.origin + '@Url.Action("SearchData", "LoadOperationRecord")';
        let method = 'Post';
        let data = { workDays: arrayWorkDays, tripName: tripName };

        // Ajax call
        $.ajax({
            url: url,
            method: method,
            data: data
        }).done(function (response) {
            var tableHTML = response.searchedTripRecordHTML;
            tableLength = response.searchedTripRecordLength + 1;
            $('#tripRecordTable').empty().html(tableHTML);
            // 日本語表示
            const language_url = "https://cdn.datatables.net/plug-ins/1.11.5/i18n/ja.json";
            const dom_structure = "<'top d-flex align-items-center'li<'ml-auto'f>>rt<'bottom'p><'clear'>";

            // ID(2列目)昇順
            $("#tripRecordDataTable").DataTable({
                "language": {
                    "url": language_url
                },
                lengthChange: true,     // 件数切替
                info: true,            // 総件数
                scrollX: true,          // 横スクロール可
                scrollCollapse: true,   // 縦スクロール表示
                searchHighlight: true,  // 検索ハイライト
                order: [[1, "asc"], [9, "asc"], [2, "asc"]],    // ID昇順
                "oLanguage": {
                    "sSearch": "フリーワード(全件)"
                },
                dom: dom_structure
            });
        }).fail(function (jqXHR, textStatus, errorThrown) {
            if (jqXHR.status === 404) {
                var errorMessage = jqXHR.responseJSON.errorMessage;
                $("#edit-modal-error-message").text(errorMessage);
            } else {
                var errorMessage = 'E3002: サーバーに接続できませんでした。' + ' HttpRequest : ' + jqXHR.status + ' textStatus : ' + textStatus;
                $("#edit-modal-error-message").text(errorMessage);
            }
        });

    }



    // 便実績テーブルの要素数
    let tableLength = 0;

    // 便実績テーブルの行情報配列
    let arrayTrs = [];

    // 到着荷量画像ボタン押下
    function OnArrivalLoadImageClick(tripRecordID, button) {
        console.log("test");
        // 配列の初期化
        arrayTrs = [];
        // 到着
        var isArrived = true;
        // テーブルに表示されている便実績のIDをリスト化
        trs = button.parentNode.parentNode.parentNode.childNodes;
        for (i = 0; i < trs.length - tableLength; i++) {
            arrayTrs.push(trs[i + tableLength].childNodes[1].textContent);
        }
        EditModal(tripRecordID, isArrived);
    }

    // 出発荷量画像ボタン押下
    function OnDepartureLoadImageClick(tripRecordID, button) {
        // 配列の初期化
        arrayTrs = [];
        // 出発
        var isArrived = false;
        // テーブルに表示されている便実績のIDをリスト化
        trs = button.parentNode.parentNode.parentNode.childNodes;
        for (i = 0; i < trs.length - tableLength; i++) {
            arrayTrs.push(trs[i + tableLength].childNodes[1].textContent);
        }
        EditModal(tripRecordID, isArrived);
    }



    // 荷量画像モーダル作成
    function EditModal(tripRecordID, isArrived) {
        // デフォルトの操作を無効化
        event.preventDefault();

        // LoadOutputModel取得
        var data = @Html.Raw(Json.Serialize(Model));
        var tripRecordModel = data.tripRecordList;
        tripRecordModel.forEach(function (item) {
            if (item.tripRecordID == tripRecordID) {

                // フォーム情報取得
                let url = window.location.origin + '@Url.Action("GetModalItems", "LoadOperationRecord")';
                let method = 'POST';
                let data = { model: item, isArrived: isArrived };

                // Ajax call
                $.ajax({
                    url: url,
                    method: method,
                    data: data
                }).done(function (response) {
                    console.log(response)
                    var imagePath = "";
                    var arriveOrDeparture = "";
                    var arriveOrDepartureDate = "";
                    var loadStatus = "";
                    var downloadFileName = "";
                    var workDayForFile = GetDayStringForFile(new Date(item.workDay));
                    var tripNameAndBranchSeq = item.tripName + "_" + item.tripBranchSeq;
                    var truckNumber = item.truckNumber;
                    if (truckNumber == "0") truckNumber = "-";
                    if (tripNameAndBranchSeq == "-_-") tripNameAndBranchSeq = "-";

                    // 到着か出発かで変わる要素の登録
                    if (isArrived) {
                        $('#detail-modal-label').text("到着荷量画像");
                        loadStatus = item.arrivalLoadStatus;
                        imagePath = response.arrivalLoadImgPath;
                        arriveOrDeparture = "到着時間";
                        arriveOrDepartureDate = GetDateString(new Date(item.arrivedAt));
                        downloadFileName = item.tripName + "_" + item.tripBranchSeq + "_" + workDayForFile + "_A_" + loadStatus + ".jpg";
                    }
                    else {
                        $('#detail-modal-label').text("出発荷量画像");
                        loadStatus = item.departureLoadStatus;
                        imagePath = response.departureLoadImgPath;
                        arriveOrDeparture = "出発時間";
                        arriveOrDepartureDate = GetDateString(new Date(item.departedAt));
                        downloadFileName = item.tripName + "_" + item.tripBranchSeq + "_" + workDayForFile + "_D_" + loadStatus + ".jpg";
                    }

                    $('#modalImage').attr("src", "data:image/jpeg;base64," + imagePath);

                    var workDay = GetDayString(new Date(item.workDay));

                    // 表示している行の上下の行の便実績ID取得
                    var tripRecordIDIndex = arrayTrs.indexOf(tripRecordID);
                    var previousTripRecordID = arrayTrs[tripRecordIDIndex - 1];
                    var nextTripRecordID = arrayTrs[tripRecordIDIndex + 1];


                    // ボタンの追加
                    var buttons = $("#tripRecord-buttons");
                    buttons.empty();
                    let div = "<div class=\"d-flex xs-block justify-content-start align-items-center p-0 mb-2 trip-record-buttons\">"
                        + "<div class=\"input-group-append mr-3\" >"
                        + " <a href=\"#\" class=\"btn btn-secondary btn-icon-split\" onclick = \"onOtherModalClick('" + previousTripRecordID + "','" + isArrived + "')\" >"
                        + "<span class=\"text\" >▲上へ</span>"
                        + "</a>"
                        + "</div>"
                        + "<div class=\"input-group-append mr-3\" >"
                        + " <a href=\"#\" class=\"btn btn-secondary btn-icon-split\" onclick = \"onOtherModalClick('" + nextTripRecordID + "','" + isArrived + "')\" >"
                        + "<span class=\"text\" >▼下へ</span>"
                        + "</a>"
                        + "</div>"
                        + "<div class=\"input-group-append mr-3 right-button\" >"
                        + " <a href=\"data:image/jpeg;base64," + imagePath + "\", download=\"" + downloadFileName + "\" class=\"btn btn-info btn-icon-split\"  >"
                        + "<span class=\"icon text-white-50\" >"
                        + "<i class=\"fa-solid fa-circle-down\" > </i>"
                        + "</span>"
                        + "<span class=\"text\" >画像出力</span>"
                        + "</a>"
                        + "</div>";
                    buttons.append(div);

                    // テーブルの追加
                    var container = $("#tripRecord-contatiner");
                    container.empty();
                    let tr = "<tr>"
                        + "<td>荷量</td>"
                        + "<td>" + loadStatus + "%</td>"
                        + "</tr><tr>"
                        + "<td>荷量の相違あり</td>"
                        + "<td>"
                        + "<div class=\"select-modal d-flex xs-block justify-content-start align-items-center p-0\">"
                        + "<select name=\"loadStatusSelect\"  class=\"form-select mr-2\" id=\"loadStatusSelect\" >"
                        + "<option value=\"\" hidden></option>"
                        + "<option value=\"1\">0%</option>"
                        + "<option value=\"3\">1-10%</option>"
                        + "<option value=\"4\">11-20%</option>"
                        + "<option value=\"5\">21-30%</option>"
                        + "<option value=\"6\">31-40%</option>"
                        + "<option value=\"7\">41-50%</option>"
                        + "<option value=\"8\">51-60%</option>"
                        + "<option value=\"9\">61-70%</option>"
                        + "<option value=\"10\">71-80%</option>"
                        + "<option value=\"11\">81-90%</option>"
                        + "<option value=\"12\">91-100%</option>"
                        + "</select>"
                        + "<a href=\"#\" class=\"btn btn-update\" onclick=\"onVerificationRequiredClick('" + item.tripRecordID + "', '" + isArrived + "')\" id=\"verificationRequired\">"
                        + "<span class=\"text\">要検証</span>"
                        + "</a>"
                        + "</div>"
                        + "</td>"
                        + "</tr><tr>"
                        + "<td>便名称_便枝番</td>"
                        + "<td>" + tripNameAndBranchSeq + "</td>"
                        + "</tr><tr>"
                        + "<td>乗務員</td>"
                        + "<td>" + item.driverName + "</td>"
                        + "</tr><tr>"
                        + "<td>ステーションID</td>"
                        + "<td>" + item.stationID + "</td>"
                        + "</tr><tr>"
                        + "<td>車両番号</td>"
                        + "<td>" + truckNumber + "</td>"
                        + "</tr><tr>"
                        + "<td>識別番号</td>"
                        + "<td>" + item.identifyNumber + "</td>"
                        + "</tr><tr>"
                        + "<td>稼働日</td>"
                        + "<td>" + workDay + "</td>"
                        + "</tr><tr>"
                        + "<td>" + arriveOrDeparture + "</td>"
                        + "<td>" + arriveOrDepartureDate + "</td>"
                        + "</tr>";
                    container.append(tr);
                    $('#loadStatusSelect').val(response.annotationLoadClass);
                }).fail(function (jqXHR, textStatus, errorThrown) {
                    if (jqXHR.status === 404) {
                        var errorMessage = jqXHR.responseJSON.errorMessage;
                        $("#edit-modal-error-message").text(errorMessage);
                    } else {
                        var errorMessage = 'E3002: サーバーに接続できませんでした。' + ' HttpRequest : ' + jqXHR.status + ' textStatus : ' + textStatus;
                        $("#edit-modal-error-message").text(errorMessage);
                    }
                });

            }
        });
    }

    // モーダル内の「上へ」「下へ」ボタン押下時
    function onOtherModalClick(otherTripRecordID, isArrived) {
        if (isArrived == "true") {
            isArrived = true;
        } else {
            isArrived = false;
        }
        EditModal(otherTripRecordID, isArrived);
    }

    // 要検証ボタン押下時
    function onVerificationRequiredClick(tripRecordID, isArrived) {
        var loadStatus = $('[name=loadStatusSelect]').val();
        if (loadStatus != "") {

            DeleteErrorMessages();


            // フォーム情報取得
            let url = window.location.origin + '@Url.Action("InsertOrUpdateAnnotationLoads", "LoadOperationRecord")';
            let method = 'POST';
            let data = { tripRecordID: tripRecordID, loadStatus: loadStatus, isArrived: isArrived };

            // Ajax call
            $.ajax({
                url: url,
                method: method,
                data: data
            }).done(function (response) {
                // 完了モーダル表示
                alert('登録が完了しました。');
            }).fail(function (jqXHR, textStatus, errorThrown) {
                if (jqXHR.status === 404) {
                    var errorMessage = jqXHR.responseJSON.errorMessage;
                    $("#edit-modal-error-message").text(errorMessage);
                } else {
                    var errorMessage = 'E3002: サーバーに接続できませんでした。' + ' HttpRequest : ' + jqXHR.status + ' textStatus : ' + textStatus;
                    $("#edit-modal-error-message").text(errorMessage);
                }
            });
        }
    }

    // 時刻の表示形式変換(HH:mm)
    function GetTimeString(date) {
        var hour = date.getHours();
        if (hour < 10) {
            hour = "0" + hour;
        }
        var minute = date.getMinutes();
        if (minute < 10) {
            minute = "0" + minute;
        }
        return hour + ":" + minute;
    }

    // 日時の表示形式変換(yyyy/MM/dd hh:mm)
    function GetDateString(date) {
        var day = GetDayString(date);
        var time = GetTimeString(date);
        return day + " " + time;
    }

    // 日付の表示形式変換(yyyyMMdd)
    function GetDayStringForFile(date) {
        var year = date.getFullYear().toString();
        var month = ("00" + (date.getMonth() + 1)).slice(-2);
        var day = ("00" + date.getDate()).slice(-2);
        return year + month + day;
    }

    // 日付の表示形式変換(MM/dd)
    function GetDayStringForChart(date) {
        var month = (date.getMonth() + 1).toString();
        var day = date.getDate().toString();
        return month + "/" + day;
    }

    // エラーメッセージ削除
    function DeleteErrorMessages() {
        $('#div-error-message').text('');
        $('#edit-modal-error-message').text('');
        $('#delete-modal-error-message').text('');
        $('.text-danger').text('');
        $('input').removeClass('input-validation-error');
    }

    // 期間が90日以内か否か
    function isWithin90Days() {
        var startOfPeriod = new Date($('#startOfPeriod').val());
        var endOfPeriod = new Date($('#endOfPeriod').val());
        var diffMilliSec = endOfPeriod - startOfPeriod;
        var diffDays = parseInt(diffMilliSec / 1000 / 60 / 60 / 24);
        if (diffDays <= 90) {
            return true;
        }
        else {
            return false;
        }
    }

    // 画像一括出力ボタン押下
    function onExportAllImages() {
        DeleteErrorMessages();
        tableDisplay();

        // 便名称取得
        var tripName = $('[name=TripName]').val();

        // フォーム情報取得
        let url = window.location.origin + '@Url.Action("ZipDownload", "LoadOperationRecord")';
        let method = 'POST';
        data = {
            download: "download",
            workDays: arrayWorkDays,
            selectedTripName: tripName
        };

        // Ajax call
        $.ajax({
            url: url,
            method: method,
            data: data
        }).done(function (response) {
            var { contentType, fileContents, fileDownloadName } = response.data;
            {
                const link = document.createElement("a");
                link.href = `data:${contentType};base64,${fileContents}`;
                link.download = fileDownloadName;
                link.click();
            }
        }).fail(function (jqXHR, textStatus, errorThrown) {
            if (jqXHR.status === 404) {
                var errorMessage = jqXHR.responseJSON.errorMessage;
                $("#div-error-message").text(errorMessage);
            } else {
                var errorMessage = 'E3002: サーバーに接続できませんでした。' + ' HttpRequest : ' + jqXHR.status + ' textStatus : ' + textStatus;
                $("#div-error-message").text(errorMessage);
            }
        });
        
    }

    // Excel出力
    async function onExportFileTrips(page, gamenName) {
        DeleteErrorMessages();
        tableDisplay();
        event.preventDefault();
        $('#div-error-message').text("");


        // 便名称取得
        var tripName = $('[name=TripName]').val();

        // フォーム情報取得
        let url = window.location.origin + '@Url.Action("ExportFile", "LoadOperationRecord")';
        let method = 'Post';
        let data = {
            "gamenName": gamenName,
            workDays: arrayWorkDays,
            tripName: tripName
        };

        // Ajax call
        $.ajax({
            url: url,
            method: method,
            data: data
        }).done(function (response) {
            console.log(response);
            var { contentType, fileContents, fileDownloadName } = response.data;
            {
                const link = document.createElement("a");
                link.href = `data:${contentType};base64,${fileContents}`;
                link.download = fileDownloadName;
                link.click();
            }

        }).fail(function (jqXHR, textStatus, errorThrown) {
            if (jqXHR.status === 404) {
                var errorMessage = jqXHR.responseJSON.errorMessage;
                $("#div-error-message").show();
                $("#div-error-message").text(errorMessage);
            } else {
                var errorMessage = 'E3002: サーバーに接続できませんでした。' + ' HttpRequest : ' + jqXHR.status + ' textStatus : ' + textStatus;
                $("#div-error-message").show();
                $("#div-error-message").text(errorMessage);
            }
        });
    }
</script>
